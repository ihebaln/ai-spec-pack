name: make data-check 
on: [push] 
jobs: 
  data-check: 
    runs-on: ubuntu-latest 
    steps: 
    - uses: actions/checkout@v4 
    - name: Run data checks 
      run: | 
        pip install pytest pandas 
        pytest tests/test_data_checks.py -v 
